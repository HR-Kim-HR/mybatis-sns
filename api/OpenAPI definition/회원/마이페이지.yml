info:
  name: 마이페이지
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/members/me"
  body:
    type: json
    data: |-
      {
        "email": "user1@example.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
